export const publicRoutes = ['/', '/new-verification', '/reset'];

export const authRoutes = ['/register', '/login', '/error'];

export const apiAuthPrefix = '/api/auth';
export const DEFAULT_LOGIN_REDIRECT = '/settings';
